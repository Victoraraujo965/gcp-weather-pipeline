name: 'weather_transform'
version: '1.0.0'

profile: 'weather_transform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_transform:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
